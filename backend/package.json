{
  "name": "membership-app-backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --config wrangler.local.jsonc",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:migrate:local": "wrangler d1 migrations apply membership-app-database --local && wrangler d1 migrations apply vms-database --local && wrangler d1 migrations apply vms-logs-database --local",
    "db:migrate:remote": "wrangler d1 migrations apply membership-app-database --remote && wrangler d1 migrations apply vms-database --remote && wrangler d1 migrations apply vms-logs-database --remote",
    "db:migrate:remote:config:local": "wrangler d1 migrations apply membership-app-database --remote --config wrangler.local.jsonc && wrangler d1 migrations apply vms-database --remote --config wrangler.local.jsonc && wrangler d1 migrations apply vms-logs-database --remote --config wrangler.local.jsonc"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "hono": "^4.12.9",
    "lib0": "^0.2.117",
    "worker-mailer": "^1.2.1",
    "y-protocols": "^1.0.7",
    "yjs": "^13.6.31",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260404.1",
    "typescript": "^6.0.2",
    "wrangler": "^4.4.0"
  }
}
