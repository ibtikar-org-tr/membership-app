{
  "name": "membership-app-backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --config wrangler.local.jsonc",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:migrate:local": "wrangler d1 migrations apply membership-app-database --local && wrangler d1 migrations apply vms-database --local",
    "db:migrate:remote": "wrangler d1 migrations apply membership-app-database --remote && wrangler d1 migrations apply vms-database --remote"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "hono": "^4.12.9",
    "worker-mailer": "^1.2.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260404.1",
    "typescript": "^6.0.2",
    "wrangler": "^4.4.0"
  }
}
